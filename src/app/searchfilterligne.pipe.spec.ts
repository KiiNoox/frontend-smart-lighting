import { SearchfilterlignePipe } from './searchfilterligne.pipe';

describe('SearchfilterlignePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfilterlignePipe();
    expect(pipe).toBeTruthy();
  });
});
