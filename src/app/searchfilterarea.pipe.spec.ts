import { SearchfilterareaPipe } from './searchfilterarea.pipe';

describe('SearchfilterareaPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfilterareaPipe();
    expect(pipe).toBeTruthy();
  });
});
